# Test / Dev environment variables
# Usage: docker compose --env-file .env.test --profile test up --build
APP_ENV=synthetic
DB_NAME=insurance_claims
DB_USERNAME=claims_user
DB_PASSWORD=claims_pass
SRC_SCHEMA=public
TGT_SCHEMA=synthetic
