spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:db}:5432/${DB_NAME:insurance_claims}
    username: ${DB_USERNAME:claims_user}
    password: ${DB_PASSWORD:claims_pass}

  jpa:
    show-sql: false

logging:
  level:
    root: WARN
    com.insurance.claim: INFO
    org.hibernate.SQL: WARN
